# 하단 탭형 GIF 비교안

- 2번 하단 탭형의 첫 번째 탭을 '이벤트'로 교체했습니다. 나머지 3개 탭은 기존 테스트 이미지 그대로입니다.
- 첫 번째 이벤트 탭에서 홈페이지의 두 GIF가 1.8초 간격, 0.6초 디졸브로 교대합니다. A안은 왼쪽 GIF부터, B안은 오른쪽 GIF부터 시작합니다.
- 원본 800x600, 4:3 비율 그대로 한 장씩 표시합니다. 3분할이나 별도 번호/화살표는 없습니다.
- 이미지 클릭은 원본 팝업의 해당 이벤트 게시글로 연결됩니다.
- GIF 내부 애니메이션과 재생 속도는 원본 그대로이며, GIF 두 개 사이에 디졸브를 적용합니다. 로딩이 끝나지 않은 GIF로 전환하지 않습니다.
- 다른 탭에서는 디졸브 전환을 멈춥니다. 상단 버튼으로 자동 전환을 정지/재생할 수 있습니다.
- 드래그 커서, 테두리, 모서리 핸들 연출은 제거했습니다. 디졸브만 유지합니다.
- 아임웹에는 선택한 안의 `-code.txt` 전체를 코드 위젯에 넣습니다. 기존 팝업 코드와 동시에 설치하지 않습니다.
- 두 GIF 안 중 하나만 설치합니다. 자동연동 3분할 버전과는 별도 비교안입니다.
- GIF URL을 직접 사용하므로 R2 복제는 하지 않았습니다. 게시판 대표 이미지 변경은 GIF 내용에 자동 반영되지 않습니다.
- 실제 홈페이지에는 설치하거나 게시하지 않았습니다. 공유 링크는 디자인 검토용입니다.
- 운영체제의 동작 줄이기는 두 이미지의 교대 및 디졸브를 정지하지만 GIF 내부 애니메이션 자체는 정지하지 않습니다.
